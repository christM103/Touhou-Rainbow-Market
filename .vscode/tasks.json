{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build", // This is the task's name
            "type": "shell",
            "command": "./build.bat", // Or "build.bat" if it's in the root
            "problemMatcher": [],
            "group": "build"
        }
    ]
}